#include <stdio.h>

int sum(int, int),i;
int main()
{
    int a,b,c;
    printf("number:");
    scanf("%d%d",&a,&b);
    c=sum (a,b);
    printf("\n the sum is %d",c);


    return 0;
}

    int sum (int x, int y)
    {
        int a,b,r;
        r=a+b;
        return r;
    }
